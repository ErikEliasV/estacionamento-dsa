# Configurações da Aplicação
server.port=${PORT:8080}
spring.application.name=estacionamento-dsa

# Origens permitidas para CORS e WebSocket (separadas por vírgula).
# Em produção (Render), defina a env CORS_ORIGINS com a URL do frontend.
app.cors.origins=${CORS_ORIGINS:http://localhost:3000,http://127.0.0.1:3000}
